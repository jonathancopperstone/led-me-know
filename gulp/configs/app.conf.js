'use strict';

module.exports = {

    /**
     * App Name, used for:
     * - concatenated (dist) files names
     * - name for src entry files (js and css)
     */

    name: '',

    /**
     * Dir where src files
     * live.
     */

    src: 'src/',

    /**
     * Dir where distributables
     * will live.
     */

    dist: 'dist/'
};
